-- PosApiKey model deferred to post-launch.
-- This stub keeps the migration sequence continuous (0020 → 0021 → 0022 → 0023 → 0024).
-- POS API key management (per-business HMAC keys, rotation, management UI)
-- will be implemented in a future wave once the core loyalty programme is live.
SELECT 1;
