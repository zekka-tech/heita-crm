import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function NotificationsPage() {
  return (
    <PlaceholderPage
      eyebrow="Customer App"
      title="Notifications"
      description="Push, WhatsApp, SMS, and in-app updates will converge into a single audit-friendly notification feed."
    />
  );
}

